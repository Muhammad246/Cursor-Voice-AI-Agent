# Cursor-Voice-AI-Agent
An advanced Voice-Controlled AI Assistant built with Python that listens to user speech, processes queries using an LLM with structured reasoning steps, optionally calls external tools, and responds using real-time text-to-speech.
